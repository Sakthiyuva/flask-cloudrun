steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/flask-cloud-run', '.']

- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/flask-cloud-run']

- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args: ['run', 'deploy', 'flask-cloud-run',
         '--image', 'gcr.io/$PROJECT_ID/flask-cloud-run',
         '--region', 'asia-south1	',
         '--platform', 'managed',
         '--allow-unauthenticated']
steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/flask-cloud-run', '.']

- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/flask-cloud-run']

- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args: [
    'run', 'deploy', 'flask-cloud-run',
    '--image', 'gcr.io/$PROJECT_ID/flask-cloud-run',
    '--region', 'asia-south1',
    '--platform', 'managed',
    '--allow-unauthenticated'
  ]

options:
  logging: CLOUD_LOGGING_ONLY
